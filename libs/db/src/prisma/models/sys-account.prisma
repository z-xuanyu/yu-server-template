model SysAccount {
  id        Int              @id @default(autoincrement())
  name      String
  email     String           @unique
  password  String
  roles     SysAccountRole[]
  createdAt DateTime         @default(now())
  updatedAt DateTime         @updatedAt

  @@map("sys_account")
}
