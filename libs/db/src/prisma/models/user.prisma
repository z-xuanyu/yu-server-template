model User {
  id        Int      @id @default(autoincrement())
  name      String
  avatar    String
  password  String
  phone     String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
